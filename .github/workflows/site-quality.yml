name: Site quality
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Check local links and page structure
        run: python3 tools/check_site.py
      - name: Check shared design and full project names
        run: python3 tools/check_design.py
      - name: Check that project titles identify the port and target
        run: python3 tools/check_project_titles.py
